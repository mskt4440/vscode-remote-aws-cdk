# vscode-remote-aws-cdk
vscode-remote templates for aws-cdk

## Configuration
- Configure your AWS credential on ~/.aws in your host machine.
- Set the AWS profile in `devcontainer.json`.
```
"remoteEnv": {
    "AWS_PROFILE": "SET_YOUR_PROFILE"
}
```

